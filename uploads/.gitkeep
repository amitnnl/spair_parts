# This file ensures the uploads/ directory is tracked by git.
# Actual uploaded files are NOT committed (they live only on the server).
# The directory must exist for file upload functionality to work.
